#include <stdio.h>
/**
 * main - Entry point
 *
 * puts: print a string
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char text[] = "\"Programming is like building a multilingual puzzle";

	puts(text);
	return (0);
}
